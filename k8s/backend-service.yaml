apiVersion: v1
kind: Service
metadata:
  name: autohealops-backend
  namespace: autohealops
  labels:
    app.kubernetes.io/name: autohealops-backend
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: autohealops-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: autohealops-backend
  ports:
    - name: http
      port: 8000
      targetPort: http
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: autohealops
  labels:
    app.kubernetes.io/name: autohealops-backend
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: autohealops-platform
    app.kubernetes.io/alias: compose-compatible
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: autohealops-backend
  ports:
    - name: http
      port: 8000
      targetPort: http
