apiVersion: v1
kind: Service
metadata:
  name: autohealops-frontend
  namespace: autohealops
  labels:
    app.kubernetes.io/name: autohealops-frontend
    app.kubernetes.io/component: web
    app.kubernetes.io/part-of: autohealops-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: autohealops-frontend
  ports:
    - name: http
      port: 8080
      targetPort: http
