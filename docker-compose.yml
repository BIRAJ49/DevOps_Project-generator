services:
  postgres:
    image: postgres:16-alpine
    container_name: devops-project-generator-postgres
    environment:
      POSTGRES_DB: devops_project_generator
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d devops_project_generator"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
